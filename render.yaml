services:
  - type: web
    name: sistema-becas
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn project.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
